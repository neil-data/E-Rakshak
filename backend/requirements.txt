fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-jose[cryptography]>=3.3.0
bcrypt>=4.0.0
python-multipart>=0.0.9
python-dotenv>=1.0.0
pydantic>=2.0
langgraph>=0.2.0
aiofiles>=23.0
groq>=0.9.0
sqlalchemy[asyncio]>=2.0
# asyncpg drives the async engine (db.py init_db / get_engine); psycopg2 drives
# the sync ORM engine behind Depends(get_db), which the live_monitoring and
# cases routers use. Both are required — the sync engine was previously
# requesting a psycopg2 driver that was never installed.
asyncpg>=0.29.0
psycopg2-binary>=2.9.9
elasticsearch>=8.13.0,<9
redis>=5.0.0
geoip2>=4.8.0
scapy>=2.5.0
-e ./static-analysis